import { Icon } from '@mui/material';
import React from 'react';
import { Typography } from '@mui/material';

export const AuthorName = () => {
  return (
    <Typography variant="body1" className='text-default'>Erica Keswin</Typography>
  );
};
