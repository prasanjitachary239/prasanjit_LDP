import { Icon } from '@mui/material';
import React from 'react';
import { Typography } from '@mui/material';

export const NumberofReads = () => {
  return (
    <Typography variant="string" >1.3k Reads</Typography>
  );
};
